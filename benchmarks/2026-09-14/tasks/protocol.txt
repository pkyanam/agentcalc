Controlled benchmark protocol. Solve the assigned task accurately using your assigned arm.
Read only this protocol, your task text, sales.csv if needed, and files in your own run directory. CLI arm may also read the installed agentcalc SKILL.md and run --help. Do not inspect product source, other runs, benchmark gold/reference results, session logs, or other agents. No network, package installation, subagents, or external services. Python3, Node, shell, and any already-installed local numeric libraries are available to the ordinary arm. The CLI arm must perform all numeric calculations and data transformations via agentcalc (its Python/Node adapters are allowed); ordinary shell/Python is allowed to orchestrate calls and format JSON. The ordinary arm must not invoke agentcalc or read its skill/help/source.

Make a reusable solution.sh in your assigned run directory, runnable with bash, which prints exactly one JSON object containing the requested answers. Use the task's inputs, not hardcoded calculated answers. Helper files may live in your own run directory. Read the CSV using its absolute path or resolve it from the script. Avoid hidden installation dependencies. Execute solution.sh and save its stdout as result.json. Fix errors you observe. Stop when done; final answer only states completed or blocked and the run directory. Do not report estimated tokens or timers; the coordinator collects those from logs. Work efficiently as you normally would; no artificial restriction on tool-call count. All results will be independently checked and the exact saved solution replayed.
